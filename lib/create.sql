-- Use the CREATE TABLE command to create a new table with various data types
CREATE TABLE bears (
    id INTEGER PRIMARY KEY,
    name TEXT,
    age INTEGER,
    gender TEXT,
    color TEXT,
    temperament TEXT,
    alive BOOLEAN
);